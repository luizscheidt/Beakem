<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Beakem</title>
  </head>
  <body>
    <h1><%= artist %> Albums</h1>
    <ul>
      <% for(let album of albums) {%>
      <li><a href="/albums/<%= album._id %>"><%= album.name %></a></li>
      <% } %>
    </ul>

    <% if(artist !== 'All') {%>
    <a href="/albums">See all albums</a>
    <% } %>
  </body>
</html>
